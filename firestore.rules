rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Internal demo only: this is not production-ready authorization.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
